#!/bin/sh
# -*- scheme -*-
exec guile $0
!#

(use-modules (nyacc lang tsh mach))
(use-modules (nyacc lalr))

(when (gen-tsh-files)
  (with-output-to-file ",file.txt"
    (lambda ()
      (pp-lalr-grammar tsh-file-spec)
      (pp-lalr-machine tsh-file-mach)))
  (with-output-to-file ",user.txt"
    (lambda ()
      (pp-lalr-grammar tsh-user-spec)
      (pp-lalr-machine tsh-user-mach))))

;;; --- last line ---
